# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

---

## フェーズ1: 設計確定

- [x] 既存UI実装をレビューし、改善方針を確定
- [x] requirements.md / design.md / tasklist.md を作成

## フェーズ2: 入力安全性とアクセシビリティ修正

- [x] `components/game-setup/LineupSection.tsx` で守備位置空値を `''` 維持に修正
- [x] `components/game-setup/LineupSection.tsx` に行文脈付き `id` / `aria-label` / `aria-describedby` を追加
- [x] `components/score-input/GameHeader.tsx` に試合進行の `sr-only` 要約を追加
- [x] `components/score-input/SubstitutionPanel.tsx` に Escape 閉じ・初期フォーカス・フォーカストラップを追加

## フェーズ3: 視覚整理

- [x] `app/globals.css` のフォント指定を Geist 変数ベースに統一
- [x] `components/score-input/ScoreLog.tsx` のコントラストを改善
- [x] `components/game/GameCard.tsx` / `components/score-input/GameHeader.tsx` のリンクタップ領域を統一

## フェーズ4: テストと検証

- [x] `components/game-setup/LineupSection.test.tsx` を追加
- [x] `npm run test -- components/game-setup/LineupSection.test.tsx` を実行
- [x] `npm run type-check` を実行
- [x] `npm run lint` を実行

---

## 実装後の振り返り

### 実装完了日
2026-02-17

### 計画と実績の差分
- 実装方針は計画通りで、画面構造は維持しながら内部実装（ARIA・キーボード操作・スタイル）を改善した。
- テスト環境では自動 cleanup が有効でなかったため、`LineupSection.test.tsx` に `afterEach(cleanup)` を追加してテスト独立性を確保した。

### 学んだこと
- React Hook の順序ルールを守るため、`SubstitutionPanel` の早期 return は Hook 宣言の後に配置する必要がある。
- 見た目の整理を優先する場合でも、先に入力値の安全性（空値→0変換）を潰すことで回帰を防ぎやすい。
